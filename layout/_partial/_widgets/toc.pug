if is_post()
  section#article-toc.widget(role='navigation')
    h3.widget-title
      | #{__('widget.toc')}
    -
      let _toc = null;
      if (page.encrypt == true) {
        _toc = toc(page.origin);
      } else {
        _toc = toc(page.content);
      }
    if _toc
      ul.widget-list
        | !{_toc}
    else
      | #{__('widget.empty')}
